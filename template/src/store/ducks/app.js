const INITIAL_STATE = []

export const Types = {}

export const Actions = {}

const reducer = (state=INITIAL_STATE, action) => {
    switch(action.type) {
        default:
            return state;
    }
}

export default reducer;